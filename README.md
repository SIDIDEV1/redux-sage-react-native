# redux-wise-react-native

You can download with git `clone` or simply download with the button and start the application with a command below:

```
cd redux-wise-react-native

npm install

start of thread

```

If you have any problem, you can run this command:

```
npm install react-native-fs --save --legacy-peer-deps
```
